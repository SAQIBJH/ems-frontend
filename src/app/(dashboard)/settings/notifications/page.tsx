import { NotificationPrefsPanel } from '@/modules/settings/components/NotificationPrefsPanel';

export const metadata = { title: 'In-app Preferences — Settings' };

export default function NotificationsPage() {
  return <NotificationPrefsPanel />;
}
