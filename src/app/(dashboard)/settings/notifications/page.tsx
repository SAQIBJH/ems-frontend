import { PlaceholderPanel } from '@/modules/settings/components/PlaceholderPanel';

export const metadata = { title: 'In-app Preferences — Settings' };

export default function NotificationsPage() {
  return (
    <PlaceholderPanel
      title="In-app Preferences"
      description="Control which in-app notifications you receive for leave updates, attendance alerts, and announcements."
    />
  );
}
