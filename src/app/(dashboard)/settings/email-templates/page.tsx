import { EmailTemplatesPanel } from '@/modules/settings';

export const metadata = { title: 'Email Templates — Settings' };

export default function EmailTemplatesPage() {
  return <EmailTemplatesPanel />;
}
