import { CompanyProfilePanel } from '@/modules/settings';

export const metadata = { title: 'Company Profile — Settings' };

export default function CompanyProfilePage() {
  return <CompanyProfilePanel />;
}
