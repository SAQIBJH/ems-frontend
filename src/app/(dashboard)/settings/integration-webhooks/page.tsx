import { WebhooksPanel } from '@/modules/settings/components/WebhooksPanel';

export const metadata = { title: 'Webhooks — Settings' };

export default function IntegrationWebhooksPage() {
  return <WebhooksPanel />;
}
