import { BillingPlanPanel } from '@/modules/settings/components/BillingPlanPanel';

export const metadata = { title: 'Plan — Settings' };

export default function BillingPlanPage() {
  return <BillingPlanPanel />;
}
