import { PlaceholderPanel } from '@/modules/settings/components/PlaceholderPanel';

export const metadata = { title: 'Working Hours — Settings' };

export default function WorkingHoursPage() {
  return (
    <PlaceholderPanel
      title="Working Hours"
      description="Define standard working hours, shift patterns, and grace periods for attendance tracking."
    />
  );
}
