import { TimesheetSettingsPanel } from '@/modules/settings/components/TimesheetSettingsPanel';

export const metadata = { title: 'Timesheets — Settings' };

export default function TimesheetSettingsPage() {
  return <TimesheetSettingsPanel />;
}
