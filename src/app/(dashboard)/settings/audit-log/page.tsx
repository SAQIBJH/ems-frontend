import { AuditLogPanel } from '@/modules/settings/components/AuditLogPanel';

export const metadata = { title: 'Audit Log — Settings' };

export default function AuditLogPage() {
  return <AuditLogPanel />;
}
