import { PlaceholderPanel } from '@/modules/settings/components/PlaceholderPanel';

export const metadata = { title: 'Audit Log — Settings' };

export default function AuditLogPage() {
  return (
    <PlaceholderPanel
      title="Audit Log"
      description="Browse a complete history of administrative actions across your organisation. (Live API — built in Step 48)"
    />
  );
}
