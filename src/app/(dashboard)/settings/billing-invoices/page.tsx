import { BillingInvoicesPanel } from '@/modules/settings/components/BillingInvoicesPanel';

export const metadata = { title: 'Invoices — Settings' };

export default function BillingInvoicesPage() {
  return <BillingInvoicesPanel />;
}
