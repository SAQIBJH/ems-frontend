import { LeaveTypesPanel } from '@/modules/settings/components/LeaveTypesPanel';

export const metadata = { title: 'Leave Types — Settings' };

export default function LeaveTypesPage() {
  return <LeaveTypesPanel />;
}
