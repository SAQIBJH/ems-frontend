import { PlaceholderPanel } from '@/modules/settings/components/PlaceholderPanel';

export const metadata = { title: 'Leave Types — Settings' };

export default function LeaveTypesPage() {
  return (
    <PlaceholderPanel
      title="Leave Types"
      description="Create and manage the leave types available to your employees — Casual, Sick, Earned, and more."
    />
  );
}
