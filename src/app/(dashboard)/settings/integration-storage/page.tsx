import { StorageIntegrationPanel } from '@/modules/settings/components/StorageIntegrationPanel';

export const metadata = { title: 'Storage Integration — Settings' };

export default function IntegrationStoragePage() {
  return <StorageIntegrationPanel />;
}
